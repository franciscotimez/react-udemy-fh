
describe('Pruebas en <DemoComponent />', () => {
    test('Esta prueba no pueba nada', () => {
        // Inicializacion
        const msg1 = 'Esta prueba no pueba nada';
        // Estimulo
        const msg2 = 'Estim';
        // Observar
        expect(msg1).toBe(msg2);

        ex;
    });
});